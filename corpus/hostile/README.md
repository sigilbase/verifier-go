# Hostile fixtures

Generated by `go run ./tools/hostile` in the verifier-go repository (github.com/sigilbase/verifier-go) from the conformance corpus; do not edit by hand. Each fixture pins one rule of FORMAT.md or one input that stopped verify.php 1.5.0 before a verdict. Expectations live in `corpus/expected.json`.

| Fixture | Pins |
| --- | --- |
| `type-object-checkpoint-hash` | checkpoint_hash is an object: FAIL (content integrity), not a fatal error |
| `type-float-actor` | a float in a hashed event field cannot be canonicalised: FAIL |
| `type-bigint-seq` | an integer beyond 2^53-1 in a hashed field cannot be canonicalised: FAIL |
| `type-object-anchor-provider` | an object where a label string is expected: still PASS, the label is cosmetic |
| `type-object-proof-size` | an object as a proof tree size casts to 1 as PHP's (int) does: the proof fails, FAIL |
| `type-scalar-signing-keys` | signing_keys is a scalar: no keys, exit 1 |
| `date-month-13` | a created_at PHP's date parser refuses: the checkpoint hash no longer recomputes and the window cannot be checked, FAIL |
| `date-trailing-newline-resigned` | the last checkpoint's created_at with one trailing newline, re-sealed: accepted by both (PCRE's $ matches before a final newline and PHP's date parser reads the value), PASS |
| `json-duplicate-keys-last-wins` | a duplicated key whose last value is the sealed one: PASS (the last value wins) |
| `json-duplicate-keys-last-differs` | a duplicated key whose last value is not the sealed one: the last value wins, the entry hash breaks, FAIL |
| `json-depth-511-resigned` | a line nested 511 containers deep (the event object plus a 510-deep payload), re-sealed: PASS (the deepest nesting PHP decodes) |
| `json-depth-512` | a line nested 512 containers deep: it does not parse, FAIL |
| `json-bom-events` | a byte order mark before the first line: that line does not parse, FAIL |
| `json-bom-manifest` | a byte order mark before manifest.json: unknown format, ERROR (exit 2) |
| `json-lone-surrogate` | a lone surrogate escape: the line does not parse, FAIL |
| `json-invalid-utf8` | an invalid UTF-8 byte inside a string: the line does not parse, FAIL |
| `json-nul-key` | an object key beginning with NUL: the line does not parse in object mode, FAIL |
| `json-float-payload` | a float in a payload: the payload cannot be canonicalised, FAIL |
| `json-int-beyond-2p53` | an integer of 2^53 in a payload: outside the exactly representable range, FAIL |
| `ndjson-crlf-blank-lines` | CRLF line endings and blank lines: PASS |
| `ndjson-form-feed-line` | a line holding only a form feed: not blank to PHP's trim, not valid JSON, FAIL |
| `json-trailing-document` | a second document after manifest.json: not valid JSON, unknown format, ERROR |
| `zip-duplicate-entry-last-wins` | events.ndjson twice, the tampered copy last: the last entry is read, FAIL |
| `zip-duplicate-entry-first-tampered` | events.ndjson twice, the tampered copy first: the last entry is read, PASS |
| `zip-parent-path-shadows` | an entry named ../events.ndjson after the real one: it extracts as events.ndjson and shadows it, FAIL |
| `zip-absolute-path-shadows` | an entry named /events.ndjson after the real one: it extracts as events.ndjson and shadows it, FAIL |
| `zip-parent-path-only` | the only events.ndjson is stored as ../events.ndjson: it extracts as events.ndjson, PASS |
| `zip-directory-collision` | a directory entry named manifest.json/: the extraction fails, exit 1 |
| `zip-extra-entries` | entries the format does not name, including a backslash name: ignored, PASS |
| `zip-folder-wrapped` | every file under a top-level folder: manifest.json is missing, exit 1 |
| `ed25519-small-order-r` | a signature whose R is the identity point and which satisfies the equation under the real key: rejected as libsodium rejects it, FAIL |
| `ed25519-non-canonical-s` | the genuine signature with S + l: rejected, FAIL |
| `ed25519-small-order-public-key` | a checkpoint signed by the identity point with a signature every RFC 8032 verifier accepts: rejected, FAIL (and the key is untrusted) |
| `anchor-token-byte-flipped` | one byte of the token changed: the token no longer matches token_hash, FAIL |
| `anchor-unknown-checkpoint` | an anchor over a checkpoint hash the bundle does not carry, FAIL |
| `anchor-bundle-roots-only` | the anchored corpus run without --tsa-roots: the built-in roots are the trusted set and the conformance TSA is not among them, so the chain does not reach a trusted root, FAIL (timestamps) |
| `key-window-retired` | the corpus key retired before the checkpoints were sealed (see keys-retired.json): FAIL (signing identity) |
| `redaction-declarations-missing` | the redacted corpus without declarations.ndjson: FAIL (redactions) |
| `redaction-declaration-names-other-event` | the declaration's targets no longer name the absent event (its hashes break too): FAIL |
